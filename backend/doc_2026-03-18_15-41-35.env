# JWT
JWT_SECRET=dev-jwt-secret-2026-student-platform
ACCESS_TOKEN_TTL=60  # 1 час для разработки
REFRESH_TOKEN_TTL=720  # 30 дней для разработки

# Database
DATABASE_URL=postgres://user:pass@localhost:5431/db?sslmode=disable

# Опционально: порт сервера
PORT=8080